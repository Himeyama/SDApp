<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SDApp.Views.GenerationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnDefinitions="360,*" Padding="20" ColumnSpacing="20">
        <StackPanel Grid.Column="0" Spacing="12" VerticalAlignment="Top">
            <TextBlock Text="Prompt" Style="{ThemeResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="PromptTextBox" AcceptsReturn="True" Height="120"
                     TextWrapping="Wrap" PlaceholderText="a cat wearing sunglasses" />

            <Button x:Name="GenerateButton" Content="Generate" HorizontalAlignment="Stretch"
                    Style="{ThemeResource AccentButtonStyle}" Click="GenerateButton_Click" />

            <TextBlock x:Name="StatusTextBlock" Text="Ready" Opacity="0.7" />
        </StackPanel>

        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8">
            <Image x:Name="ResultImageControl" Stretch="Uniform" Margin="8" />
        </Border>
    </Grid>
</Page>
